// Placeholder TanStack Table component for templates
// Full impl: columns for name, desc, tags, actions (load/delete/share)
import { DataTable } from './ui/data-table';
// Define columns here with actions

export { DataTable as TemplatesTable };
// Note: Expand with full columns in next iter if needed